return {
    "nvim-lua/plenary.nvim", -- lua functions that many plugins use
    "christoomey/vim-tmux-navigator", -- tmux & split window navigation (navigate <ctrl>+(h,j,k,l))  (open <leader>s(h,v,x))
}
